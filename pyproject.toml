[tool.poetry]
name = "matrix-meta"
version = "0.1.0"
description = "Matrix-Meta is a suite of services designed to integrate the Matrix protocol, enabling seamless interoperability."
authors = ["amirtaherkhani <amirtaherkhani@outlook.com>", "parhamya1 <parham.ya1@gmail.com>"]
license = "GPL-3.0"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
django = "^5.1.3"
django-unfold = "^0.42.0"
pylint = "^3.3.1"
typer = {extras = ["all"], version = "^0.13.1"}
dynaconf = {extras = ["django"], version = "^3.2.6"}
djangorestframework = "^3.15.2"


[tool.poetry.group.dev.dependencies]
commitizen = "^4.0.0"
pre-commit = "^4.0.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
scripts = "scripts:main"

[tool.cz]
name = "cz_conventional_commits"
version = "0.0.1"  # Replace with your project's current version
tag_format = "v{version}"  # Tag format
changelog_file = "CHANGELOG.md"  # Define the changelog file path


[tool.poetry-dynamic-versioning]
enable = true  # Enables dynamic versioning
pattern = "v(?P<version>.*)"  # Matches tags starting with "v", e.g., "v1.2.0"
dirty = true  # Optionally append ".dirty" if the working tree is modified



